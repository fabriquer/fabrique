#
# RUN: %fab --format=null --print-dag %s > %t.out
# RUN: FileCheck %s -input-file %t.out
#

# CHECK-DAG: foo:maybe[int] = {
# CHECK-DAG:   exists:bool = true
# CHECK-DAG:   value:int = 42
foo = some(42);

# CHECK-DAG: bar:bool = true
bar = foo.exists;

# CHECK-DAG: baz:int = 42
baz = foo.value;
