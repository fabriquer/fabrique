#
# RUN: %fab --format=null --print-dag %s > %t
# RUN: %check %s -input-file %t
#

nonsense = file('foo/bar/wibble');

# CHECK-DAG: foo:file = tmproot/include
foo = file('tmproot/include');

# CHECK-DAG: bar:file = tmproot/include
bar = file('tmproot') + '/include';

# CHECK-DAG: baz:file = tmproot/include
baz = file('tmproot') + '/include/';
