foo = 'Hello, world!';

bar = action('${BAR} ${src} -o ${dest}' <- src:file[in], dest:file[out]);

baz = function(x:int): int x + 1;

wibble = function(x:int): int
	baz(x)
	;

subsubimport = import('subsubmodule.fab');
