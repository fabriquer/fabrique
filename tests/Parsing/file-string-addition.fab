#
# RUN: %fab --parse-only --print-ast %s > %t
# RUN: FileCheck %s -input-file %t
#

# CHECK-DAG: foo = file('foo.txt');
foo = file('foo.txt');

# CHECK-DAG: bar:file = foo + '.gz';
bar:file = foo + '.gz';
