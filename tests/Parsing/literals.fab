#
# RUN: %fab --parse-only --print-ast %s > %t
# RUN: FileCheck %s -input-file %t
#

# CHECK: foo:bool = true;
foo = true;

# CHECK: bar:int = 42;
bar:int = 42;

# CHECK: baz:list[int] = [ 1 2 3 ];
baz = [ 1 2 3 ];

# CHECK-DAG: wibble:list[string] = [ 'some' "strings" ];
wibble = [ 'some' "strings" ];
