#
# RUN: %fab --parse-only --print-ast %s > %t
# RUN: FileCheck %s -input-file %t
#

foo = record
{
	bar = record
	{
		baz = record
		{
			wibble = 1;
		};
	};
};

# CHECK: the_wibble:int = foo.bar.baz.wibble;
the_wibble = foo.bar.baz.wibble;
