#
# RUN: %fab --parse-only --print-ast %s > %t
# RUN: FileCheck %s -input-file %t
#

# CHECK: foo:bool = true;
foo = true;

# CHECK: bar:bool = not foo;
bar = not foo;

# CHECK: baz:bool = foo or bar;
baz = foo or bar;

# CHECK: wibble:bool = foo and bar;
wibble = foo and bar;

# CHECK: wobble:bool = foo and bar or baz xor wibble;
wobble = foo and bar or baz xor wibble;
