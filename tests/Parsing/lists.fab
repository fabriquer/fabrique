#
# RUN: %fab --parse-only --print-ast %s > %t
# RUN: FileCheck %s -input-file %t
#

# CHECK: default_cflags = [ 'a' 'list' 'of' 'strings' ];
default_cflags = [ 'a' 'list' 'of' 'strings'];

# CHECK: some_numbers = [ 1 2 3 ];
some_numbers = [ 1 2 3 ];

# CHECK: singleton = [ '!' ];
singleton = [ '!' ];

# CHECK: nothing:list[nil] = [ ];
nothing:list[nil] = [ ];
