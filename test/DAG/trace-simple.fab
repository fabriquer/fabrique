#
# Without `--debug='trace'` we should still see minimal printed messages,
# but no filename information, etc.
#
# RUN: fab --format=null %s > %t
# RUN: FileCheck %s -input-file %t
#

# CHECK-NOT: [[DIR:.*]]/trace.fab:8:5: info: debug trace point
# CHECK-NOT: value: 42
# CHECK: 42
x = print(42);

# CHECK-NOT: [[DIR:.*]]/trace.fab:12:15: info: debug trace point
# CHECK-NOT: value: [ 3 4 5 6 ]
# CHECK: [ 3 4 5 6 ]
y = [ 1 2 ] + print([ 3 4 ] + [ 5 6 ]);
