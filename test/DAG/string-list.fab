#
# RUN: %fab --print-dag --format=null %s > %t
# RUN: FileCheck %s -input-file %t
#

foo = 'a string literal';
bar = 'and another one';

# CHECK-DAG: list[string] baz = [ 'a string literal' 'and another one' ]
baz = [ foo bar ];

# CHECK-DAG: list[string] wibble = [ 'yes, it's' 'a string literal' 'from a list' ]
wibble = [ "yes, it's" foo 'from a list' ];
