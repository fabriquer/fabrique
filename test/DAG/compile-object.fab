#
# RUN: fab --print-dag --format=null %s > %t
# RUN: FileCheck %s -input-file %t
#

# CHECK-DAG: file src = Inputs/foo.c
src = file('Inputs/foo.c');

# CHECK-DAG: (file)=>file cc = cc -c -D $foo $in -o $out
cc = action('cc -c -D $foo $in -o $out' <- in:file[in], out:file[out], foo:string);

# CHECK-DAG: build: cc { Inputs/foo.c => foo.o }( foo = 'SOME_DEFINITION' )
# CHECK-DAG: file obj = foo.o
obj = cc(src, out = file('foo.o'), foo = 'SOME_DEFINITION');
