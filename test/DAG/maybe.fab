#
# RUN: %fab --format=null --print-dag %s > %t.out
# RUN: FileCheck %s -input-file %t.out
#

# CHECK-DAG: maybe[int] foo = {
# CHECK-DAG:   bool exists = true
# CHECK-DAG:   int value = 42
foo = some(42);

# CHECK-DAG: bool bar = true
bar = foo.exists;

# CHECK-DAG: int baz = 42
baz = foo.value;
