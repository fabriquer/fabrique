#
# RUN: %fab --print-dag --format=null %s > %t
# RUN: FileCheck %s -input-file %t
#

# CHECK-DAG: list[nil] empty = [ ]
empty = [];

# CHECK-DAG: list[int] integers = [ 1 2 3 ]
integers = [ 1 2 3 ];

# CHECK-DAG: list[string] strings = [ 'hello' 'world' ]
strings = [ 'hello' 'world' ];

# CHECK-DAG: list[file] fileseq = [ a.c b.c ]
fileseq = [ file('a.c') file('b.c') ];

# CHECK-DAG: list[file] morefiles = [ a.c b.c ]
morefiles = files(a.c b.c);
