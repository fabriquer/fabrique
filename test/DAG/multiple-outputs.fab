#
# RUN: fab --print-dag --format=null %s > %t
# RUN: FileCheck %s -input-file %t
#

# CHECK-DAG: (file)=>list[file] lex = $LEX -c++ --header-file=$header --outfile=$out $in
lex = action('$LEX -c++ --header-file=$header --outfile=$out $in'
              <- in:file, out:file, header:file[out]);

# CHECK-DAG: build: lex { foo.yy => foo.c + foo.h }( header = foo.h )
# CHECK-DAG: list[file] gen = [ foo.c foo.h ]
gen = lex(file('foo.yy'), file('foo.c'), header = file('foo.h'));
