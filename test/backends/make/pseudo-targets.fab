#
# RUN: fab --format=make --output=%t %s
# RUN: FileCheck %s -input-file %t/Makefile
#

# CHECK: foo=	a string
foo = 'a string';

#
# If a value is a file or list of files, it should generate a psuedo-target:
#

# CHECK: bar : ${srcroot}/a.c
bar = file('a.c');

# CHECK: baz : ${srcroot}/b.c ${srcroot}/c.c
baz = files(b.c c.c);
