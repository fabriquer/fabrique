#
# RUN: %fab --parse-only --print-ast -D foo=42 %s > %t
# RUN: FileCheck %s -input-file %t
#

# CHECK: foo:int = args.foo;
foo = args.foo;

# CHECK: bar:string = args.bar ? 'default bar';
bar = args.bar ? 'default bar';
