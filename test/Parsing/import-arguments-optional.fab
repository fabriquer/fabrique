#
# RUN: %fab --parse-only --print-ast %s > %t
# RUN: FileCheck %s -input-file %t
#

# CHECK: no_args:struct[name:string] = import('Inputs/accepts-args.fab');
no_args = import('Inputs/accepts-args.fab');

# CHECK: optional_args:struct[name:string] = import('Inputs/accepts-args.fab');
optional_args = import('Inputs/accepts-args.fab');

# CHECK: name:string = optional_args.name;
name = optional_args.name;
