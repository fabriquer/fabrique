#
# RUN: fab --parse-only --print-ast %s 2> %t || true
# RUN: FileCheck %s -input-file %t
#

cc = action('cc -c $in -o $out' <- in:file, out:file);

# CHECK: {{.*}}.fab:[[line:[0-9]+]]:{{.*}} missing argument to 'in'
obj = cc(out = file('foo.c'));

# CHECK: [[line]] obj = cc(out = file('foo.c'));
# CHECK-NEXT:           ^~~~~~~~~~~~~~~~~~~~~~~
