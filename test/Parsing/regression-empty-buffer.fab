#
# This is a reduced test case for an empty lex buffer we used to have.
#
# RUN: fab --parse-only --print-ast %s > %t
# RUN: FileCheck %s -input-file %t
#
# XFAIL: *
#

foo = function(x:string, y:string)
	if (x == '') y
	else x
;
