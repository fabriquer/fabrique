#
# RUN: fab --parse-only --print-ast %s > %t
# RUN: FileCheck %s -input-file %t
#

# CHECK: foo:struct[string, string] = struct
# CHECK-NEXT: {
# CHECK-DAG:   foo:string = 'hi';
# CHECK-DAG:   bar:string = 'world';
foo = struct { foo = 'hi'; bar = 'world'; };

# CHECK: bar:string = foo.bar;
bar = foo.bar;
