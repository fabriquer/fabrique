#
# RUN: %fab --format=null --print-dag %s > %t
# RUN: FileCheck %s -input-file %t
#

sysctl = import('sysctl');

# CHECK-DAG: string ostype =
ostype = sysctl.string('kern.ostype');

# CHECK-DAG: int osrevision =
osrevision = sysctl.int('kern.osrevision');
